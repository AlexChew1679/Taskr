<h1> Edit Task </h1>

<% if @task.errors.any? %>
<h4> Oopps u fuck up! </h4>
  <ul>
  <% @task.errors.full_messages.each do |msg| %>
  <li><%= msg %></li>
  <%end%>
  </ul>
<%end%>

<%= simple_form_for @task do |f| %>
  <%= f.input :content %>
  <%= f.button :submit %>

<% end %>

<%= link_to 'Back to All Task', tasks_path %>

<%= link_to 'Back to New Task', new_task_path %>
